<?xml version="1.0" encoding="UTF-8"?>
<!--
  Copyright © 2026 Khrustal & Mann
               MELBOURNE, VICTORIA, AUSTRALIA, 3000

  Licensed under the Apache License, Version 2.0 (the "License");
  you may not use this file except in compliance with the License.
  You may obtain a copy of the License at

      http://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing, software
  distributed under the License is distributed on an "AS IS" BASIS,
  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or
  implied. See the License for the specific language governing
  permissions and limitations under the License.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.p2pmsgcore</groupId>
  <artifactId>p2pmsgcore-java</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <packaging>jar</packaging>
  <description>Java Panama FFI bindings for the P2Pmsgcore native library</description>

  <properties>
    <maven.compiler.release>22</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <!-- Directory holding P2Pmsgcore.dll (and the Msgcore.dll it imports).
         The native sources live in the P2Pmsgcore repository, not this one, so
         this points into a checkout of it; override per site:
             mvn test -Dp2pmsgcore.lib.dir=D:\path\to\P2Pmsgcore\out\x64\Release
         It used to default to ../native/build, back when this tree carried its
         own copy of the C wrapper. That copy was deleted on 2026-08-14. -->
    <p2pmsgcore.lib.dir>${project.basedir}/../../../MSCS/P2Pmsgcore/out/x64/Release</p2pmsgcore.lib.dir>
  </properties>

  <build>
    <plugins>

      <!-- Compile with enable-preview if needed; at minimum release 22 -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
        <configuration>
          <release>22</release>
        </configuration>
      </plugin>

      <!-- Pass enable-native-access for tests.
         NOTE java.library.path below no longer locates the DLL: jextract 25's
         output uses SymbolLookup.libraryLookup, which goes through the OS loader
         search (exe dir, System32, PATH) and never consults it. run_all.ps1 sets
         PATH instead. The property is kept because it costs nothing and a future
         hand-written loader would want it. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.2.5</version>
        <configuration>
          <argLine>--enable-native-access=ALL-UNNAMED</argLine>
          <systemPropertyVariables>
            <!-- Adjust to the directory that holds P2Pmsgcore.dll -->
            <java.library.path>${p2pmsgcore.lib.dir}</java.library.path>
          </systemPropertyVariables>
        </configuration>
      </plugin>

      <!-- Executable jar with SmokeTest as main class -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <version>3.4.1</version>
        <configuration>
          <archive>
            <manifest>
              <mainClass>com.p2pmsgcore.SmokeTest</mainClass>
            </manifest>
          </archive>
        </configuration>
      </plugin>

    </plugins>
  </build>
</project>
